.env
*.db
__pycache__/
*.pyc
.playwright/

# Bridge daemon runtime files
bridge.log
bridge.log.*
bridge.startup.log
bridge.watchdog.log
.commands/

# Local capture corpus (staged for ingestion to Postgres, not for git)
data/snapshots/

# Scheduled-task wrapper log files (one per day)
logs/
.claude/

# Bot-state export dumps — contain full positions/trades/PnL (credentials are
# redacted, but holdings are NOT). Generated into the repo tree; never commit.
bot_state_export_*.md

commit*.patch
_holdings_cache.json
