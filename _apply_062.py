"""SUPERSEDED (v4.1 calibration pass, 2026-07-11) — never committed.
The TRANCHE v2 rollout now lives in _apply_063.py, which applies BOTH
migrations (062 position_targets + 063 cash_equivalent) and runs the
calibrated dry run. Use:
    python _apply_063.py
"""
import sys
sys.exit("superseded — run: python _apply_063.py")
